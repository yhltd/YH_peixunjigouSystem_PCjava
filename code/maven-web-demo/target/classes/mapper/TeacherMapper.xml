<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yhocn.teacher.mapper.TeacherMapper">

	<select id="selectAll" parameterType="Teacher" resultType="Teacher">
		select * from teacher where Company =#{c}
	</select>
	<select id="selectAll1" parameterType="Teacher" resultType="Teacher">
		select distinct Company from teacher
	</select>
	<select id="login" resultType="Teacher">
		select *
		from teacher
		where
		UserName=#{userName} and Password=#{password} and Company=#{Company}
	</select>

	<insert id="add">
		insert into
		teacher(UserName,Password,RealName,UseType,Age,
		Phone,Home,photo,Education,Company )
		values(#{userName},#{password},#{realName},#{useType},#{age},
		#{phone},#{home},#{photo},#{education},#{c})
	</insert>
	
	<update id="update">
		update teacher
		set
		RealName=#{realName},
		Age=#{age},
		Phone=#{phone},
		Home=#{home},
		photo=#{photo},
		Education=#{education}
		Company =#{c}
		where
		ID=#{id} and Company =#{c}
	</update>
	<update id="alter">
		update teacher
		set
		UserName=#{userName},
		Password=#{password},
		RealName=#{realName},
		UseType=#{useType},
		Age=#{age},
		Phone=#{phone},
		Home=#{home},
		photo=#{photo},
		Education=#{education}
		Company =#{c}
		where
		ID=#{id} and Company =#{c}
	</update>
	<delete id="delete">
		delete from teacher
		where ID=#{id} and Company =#{c}
	</delete>
	<select id="selectById" resultType="Teacher">
		select * from teacher where
		ID=#{id} and Company =#{c}
	</select>
</mapper>
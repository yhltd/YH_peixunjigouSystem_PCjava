<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yhocn.teacher.mapper.TeacherMssqlMapper">

    <select id="selectAll3" resultType="java.lang.String">
        select distinct Company from teacher
        where Company != '' and Company is not null
    </select>

</mapper>
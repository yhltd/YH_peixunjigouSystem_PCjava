<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yhocn.shezhi.mapper.ShezhiMapper">

	<select id="selectAll" parameterType="Shezhi" resultType="Shezhi">
		select * from shezhi where Company =#{c}
	</select>


	<insert id="add">
		insert into
		shezhi(course,teacher,type,paiment,msort,psort,Company)
		values(#{course},#{teacher},#{type},#{paiment},#{msort},#{psort},#{c})
	</insert>

	<update id="update">
		update shezhi
		set
		Course=#{course},
		Teacher=#{teacher},
		Type=#{type},
		Paiment=#{paiment},
		Msort=#{msort},
		Psort=#{psort}
		Company =#{c}
		where
		ID=#{id} and Company =#{c}
	</update>
	<update id="alter">
		update shezhi
		set
		Course=#{course},
		Teacher=#{teacher},
		Type=#{type},
		Paiment=#{paiment},
		Msort=#{msort},
		Psort=#{psort}
		Company =#{c}
		where
		ID=#{id} and Company =#{c}
	</update>
	<delete id="delete">
		delete from shezhi
		where ID=#{id} and Company =#{c}
	</delete>
	<select id="selectById" resultType="Shezhi">
		select * from shezhi where
		ID=#{id} and Company =#{c}
	</select>
</mapper>